export const Faq = () => {
  // const isLoggedIn = useAppSelector(selectIsLoginIn)
  //
  // if (!isLoggedIn) {
  //   return <Navigate to={PATH.Login} />
  // }

  return (
    <div>
      FAQ
    </div>
  )
}

export default Faq